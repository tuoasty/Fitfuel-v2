export default function CompleteProfile() {
    return (
        <main>
            {/*Todo -> Create a second register page for inputting profile data, such as weight, height, date of birth, and also diet and activty enum (Check schema.prisma)*/}
            Complete Profile
        </main>
    )
}